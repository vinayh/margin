/**
 * "Margin" brand mark in Bagel Fat One. Tailwind v4 generates the
 * `font-display` utility from `--font-display` in `shared/tokens.css`;
 * the matching `@font-face` is in `src/api/styles/input.css`.
 */
export function BrandMark() {
  return <h1 class="font-display text-4xl leading-tight tracking-tight">Margin</h1>;
}
