---
interface NavItem {
  href: string;
  label: string;
}
interface Props {
  items: NavItem[];
}
const { items } = Astro.props;
---

<aside class="hidden md:block w-48 shrink-0 sticky top-0 self-start h-dvh py-10 pl-8 pr-4">
  <a href="/" class="flex items-baseline gap-2 mb-1">
    <span class="font-display text-3xl leading-none">margin</span>
  </a>
  <div class="text-[11px] tracking-wide text-muted uppercase mb-10">
    v0.1.0 · alpha
  </div>

  <nav class="text-[13px]">
    <ul class="space-y-1.5">
      {items.map((item) => (
        <li>
          <a
            href={item.href}
            class="text-ink-2 hover:text-ink hover:underline underline-offset-4 decoration-2 decoration-accent"
          >
            {item.label}
          </a>
        </li>
      ))}
    </ul>

    <div class="mt-10 mb-3 text-[11px] tracking-wide text-muted uppercase">
      Links
    </div>
    <ul class="space-y-1.5">
      <li>
        <a
          href="https://github.com/vinayh/margin"
          class="text-ink-2 hover:text-ink"
          >GitHub</a
        >
      </li>
      <li>
        <a
          href="mailto:hello@margin.pub"
          class="text-ink-2 hover:text-ink"
          >Contact</a
        >
      </li>
    </ul>
  </nav>
</aside>
